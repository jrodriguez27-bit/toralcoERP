# Base de datos (usado internamente por docker-compose, la app usa DATABASE_URL construido en el compose)
POSTGRES_PASSWORD=CAMBIAR_PASSWORD_SEGURO_AQUI

# NextAuth - OBLIGATORIO cambiar en produccion
NEXTAUTH_SECRET=CAMBIAR_SECRET_SEGURO_AQUI

# Dominio (ya configurado en docker-compose.prod.yml)
# NEXTAUTH_URL=https://toralcoerp.quantium.com.do
